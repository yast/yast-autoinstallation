/**
 * File:
 *   cfg_autoinstall.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/autoinstall
 *   using the sysconfig-agent
 * 
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/autoinstall</tt>
 * in an easy manner.
 */
.sysconfig.autoinstall

`ag_ini(
  `SysConfigFile("/etc/sysconfig/autoinstall")
)
