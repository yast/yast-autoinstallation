/**
 * File:
 *   yast1sel.scr
 * Summary:
 *   SCR Agent for reading Package selection from yast1 style selection files
 * Access:
 *   read-only
 * Authors:
 *   Anas Nashif <nashif@suse.de>
 * See:
 *   libscr
 * Example:
 *   Read(.yast1sel.selection,"<filename>")
 * 
 * $Id$
 * Converts a package selection file from the old YaST syntax to
 * the new YaST2 syntax.
 *
 */
.yast1sel

`ag_yast1sel ()
