/**
 * File:
 *   tmp_alice_part.scr
 * Summary:
 *   SCR Agent for reading yast1/alice partition plans
 * Access:
 *   read
 * Authors:
 *   Unknown <yast2-hacker@suse.de>
 * See:
 *
 *   eth0 10.70.123.14    255.255.0.0 10.70.123.255
 *   anyagent
 *   libscr
 */
.tmp.alice.network

`ag_anyagent (
    SCR::AnyAgent  (
        ``(File ("/tmp/alice_network")),
        "#\n",
        true,
        ``(
            Tuple(
                Optional(Whitespace()),
                Name(String("^\t\n ")),
                Whitespace(),
                Value (
                    Or(
                        Tuple(
                            `ipaddr(Ip4Number()),
                            Whitespace(),
                            `netmask(Ip4Number()),
                            Whitespace(),
                            `broadcast(Ip4Number())
                            ),
                        Tuple(
                            `bootproto(String("^\n\t "))
                            )
                        )
                    ),
                Continue ("\n")
                )
            )
        )
    )

